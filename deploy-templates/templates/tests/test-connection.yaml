apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "backend-balancing.fullname" . }}-test-connection"
  labels:
    {{- include "backend-balancing.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "backend-balancing.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
